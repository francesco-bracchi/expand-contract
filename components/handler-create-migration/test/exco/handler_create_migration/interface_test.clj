(ns exco.handler-create-migration.interface-test
  (:require [clojure.test :as test :refer :all]
            [exco.handler-create-migration.interface :as handler-create-migration]))

(deftest dummy-test
  (is (= 1 1)))
