(ns exco.handler-rename-table.interface-test
  (:require [clojure.test :as test :refer :all]
            [exco.handler-rename-table.interface :as handler-rename-table]))

(deftest dummy-test
  (is (= 1 1)))
