(ns exco.handler-init-migrator.interface-test
  (:require [clojure.test :as test :refer :all]
            [exco.handler-init-migrator.interface :as handler-init-migrator]))

(deftest dummy-test
  (is (= 1 1)))
