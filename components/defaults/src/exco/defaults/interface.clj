(ns exco.defaults.interface)

(def directory
  ".exco")

(def workspace-file
  "workspace.edn")


(def migrations-dir
  "migrations")

(def project
  :main)

(def migration
  'initialize)

(def color
  true)
