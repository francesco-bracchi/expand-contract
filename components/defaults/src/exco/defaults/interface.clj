(ns exco.defaults.interface)

(def directory
  ".exco")

(def workspace-file
  "workspace.edn")


(def migrations-dir
  "migrations")

(def db
  :main)

(def migration
  'initialize)

(def color
  true)
