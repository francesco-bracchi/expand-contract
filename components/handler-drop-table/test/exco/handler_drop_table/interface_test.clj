(ns exco.handler-drop-table.interface-test
  (:require [clojure.test :as test :refer :all]
            [exco.handler-drop-table.interface :as handler-drop-table]))

(deftest dummy-test
  (is (= 1 1)))
