(ns exco.handler-drop-column.interface-test
  (:require [clojure.test :as test :refer :all]
            [exco.handler-drop-column.interface :as handler-drop-column]))

(deftest dummy-test
  (is (= 1 1)))
