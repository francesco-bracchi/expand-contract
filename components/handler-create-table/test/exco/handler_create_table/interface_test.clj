(ns exco.handler-create-table.interface-test
  (:require [clojure.test :as test :refer :all]
            [exco.handler-create-table.interface :as handler-create-table]))

(deftest dummy-test
  (is (= 1 1)))
