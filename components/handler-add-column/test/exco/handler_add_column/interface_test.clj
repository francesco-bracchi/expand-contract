(ns exco.handler-add-column.interface-test
  (:require [clojure.test :as test :refer :all]
            [exco.handler-add-column.interface :as handler-add-column]))

(deftest dummy-test
  (is (= 1 1)))
